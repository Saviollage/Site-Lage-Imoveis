import 'package:flutter/material.dart';

class PropertiesPageMobile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('PropertyPageMobile'),
      ),
    );
  }
}
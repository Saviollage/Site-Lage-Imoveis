import 'package:flutter/material.dart';

class SimulationPageMobile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('SimulationPageMobile'),
      ),
    );
  }
}
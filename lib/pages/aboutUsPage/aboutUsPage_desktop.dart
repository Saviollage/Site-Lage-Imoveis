import 'package:flutter/material.dart';

class AboutUsPageDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('AboutUsPageDesktop'),
      ),
    );
  }
}